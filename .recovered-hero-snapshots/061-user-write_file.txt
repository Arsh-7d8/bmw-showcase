META {"line":845,"id":"db9266a3-8341-401f-bb6a-9286a49b0bd2","type":"user","timestamp":"2026-06-01T07:08:10.555Z"}
TOOL write_file
ID write_file__write_file_1780297689591_0

Successfully created and wrote to new file: C:\Users\balji\.gemini\tmp\balji\a4bc24a1-e5c1-444e-b121-e9504379bf56\plans\restore-cinematic-scroll.md. Here is the updated code:
# Restoration Plan: Cinematic Scroll & Mask Precision

## Objective
Restore the "cinematic" feel to the scroll interactions, fix the alignment and transparency of the "COMPETITION" mask, and ensure the overall pace is deliberate rather than "ultra fast".

## Diagnostics
1.  **Ultra Fast Scroll**: Removing the `800vh` scroll container removed the "resistance" and pacing of the Hero section. We need a tall container to slow the user down and create a sense of scale, even if the primary entrance is partially time-based or a hybrid. Furthermore, we must check if `SmoothScroll` (Lenis) is functioning correctly and perhaps increase its `duration` or `damping` for a heavier, more cinematic feel.
2.  **Broken Mask**:
    *   **Transparency/Effect**: The user wants a *mask*. We changed it to just text with opacity (`text-white/25`) because the CSS `mask-image` wasn't working correctly over the video. We need to revert to a true mask effect where the video is seen *through* the text. We will use a `mix-blend-mode: multiply` on a white background with black text, or a true CSS mask-image if implemented correctly. Let's try the `mix-blend-mode` approach for a reliable knockout effect.
    *   **Alignment**: The "COMPETITION" text was scaling up, but if it wasn't perfectly centered, it would look misaligned at large scales. `transformOrigin: "center center"` is critical.

## Strategy: The Hybrid "Cinematic Lock"

We will rebuild the Hero to use a scroll-locked, cinematic sequence. It won't be 800vh, but maybe 400vh to give the user time to "scroll through" the opening sequence, restoring the "angle" and "animation" the user misses.

### Implementation Steps

1.  **Re-introduce Cinematic Scroll (Hero.tsx):**
    *   Make the Hero container `h-[400vh]`.
    *   Use `sticky top-0 h-screen overflow-hidden` for the inner content.
    *   Re-map `useTransform` to control the sequence based on `scrollYProgress`. This is what provides the "cinematic angle" and animation tied to user intent.

2.  **Fix the "COMPETITION" Knockout Mask:**
    *   We will use a solid white div covering the screen with `mix-blend-mode: multiply`.
    *   Inside it, the word "COMPETITION" will be pure black. Because of `multiply`, white becomes transparent (showing the video), and black becomes a solid blocker. Wait, no. We want the video to show *through* the text, and the rest to be solid dark.
    *   **Correct Knockout Method**: Solid black background. The text "COMPETITION" is white. Use `mix-blend-mode: screen` on the video layer above it, OR use a CSS mask. Let's use a robust CSS mask technique:
        ```html
        <motion.div 
           className="absolute inset-0 bg-black flex items-center justify-center text-[15vw] font-black uppercase text-white"
           style={{ WebkitTextFillColor: "transparent", WebkitTextStroke: "1px white", ... }}
        >
           COMPETITION
        </motion.div>
        ```
        Actually, the most reliable way to make a mask that scales is an SVG mask or a massive scale transform on text with `mix-blend-mode`. Let's use a black overlay with transparent text cut out of it. We can do this by overlaying a massive white block over the video, putting black text on it, and using `mix-blend-multiply` to knock out the white? No.
        Let's stick to the CSS mask approach, but simplified: `mask-image: url('...'); mask-size: cover;`. Or simpler: Just massive text that scales up, filling the screen. Let's use `mix-blend-overlay` or similar, or just a stark outline text (`text-transparent stroke-white`). The user wants a "mask".

    *   **Let's try the Inverse Knockout:** A black div (`bg-black`) covering everything. Inside it, a `<motion.div>` with white text. Apply `mix-blend-multiply` to the text layer. This makes the white text transparent, showing the video behind the black div. Yes, this works.
    *   Let's refine the mask implementation in `Hero.tsx`.

3.  **Heavier Smooth Scroll (SmoothScroll.tsx):**
    *   If Lenis is used, increase `duration` to `1.5` or `2.0` and lower the easing curve (e.g., `easing: (t) => Math.min(1, 1.001 - Math.pow(2, -10 * t))`) to make scrolling feel heavier and more deliberate.

4.  **Header Animation Restoration:**
    *   Ensure `Navbar.tsx` enters gracefully but only *after* the initial Hero sequence is scrolled past. If Hero is `400vh`, then trigger Navbar after `scrollY > window.innerHeight * 1.5`.

## Sequence of Operations
1. Modify `components/SmoothScroll.tsx` to increase scroll weight.
2. Rewrite `components/Hero.tsx` to restore the `h-[400vh]` sticky structure and implement the `mix-blend` knockout mask.
3. Update `components/Navbar.tsx` to trigger correctly based on the new scroll depth.