META {"line":796,"id":"22471a5d-5ff7-46dd-bfb7-502ee1838fbb","type":"user","timestamp":"2026-06-01T06:58:34.427Z"}
TOOL write_file
ID write_file__write_file_1780297113577_0

Successfully created and wrote to new file: C:\Users\balji\.gemini\tmp\balji\a4bc24a1-e5c1-444e-b121-e9504379bf56\plans\revert-hero-scroll.md. Here is the updated code:
# Revert: Time-Based Hero Entrance

## Objective
Decouple the `Hero.tsx` entrance sequence (Logo, Shutters, COMPETITION mask) from the scroll progress, converting it back to a time-based entry animation.

## Diagnostics & Strategy
The current implementation maps everything to `scrollYProgress` using `useTransform`. This forces the user to scroll to see the animation. The user wants the original behavior where the animation plays out on load, and then the page becomes scrollable or the rest of the content is revealed.

We will:
1.  **Remove Scroll Dependencies:** Strip out `useScroll`, `useSpring`, and all `useTransform` hooks from `Hero.tsx` related to the entry sequence.
2.  **Implement Sequence State:** Use React state or Framer Motion variants/orchestration to control the flow:
    *   Phase 1: Logo Entrance.
    *   Phase 2: Shutters Open.
    *   Phase 3: COMPETITION mask scale up and fade out.
    *   Phase 4: UI Reveal and unlock scrolling (if applicable) or trigger `onComplete`.
3.  **Adjust Container:** Remove the `h-[800vh]` and `sticky top-0 h-screen` logic, replacing it with a standard full-height or controlled height section since we no longer need scroll real estate for the animation.

## Implementation Steps (`Hero.tsx`)

1.  **Remove Scroll Hooks:** Delete `useScroll`, `useSpring`, `useMotionValueEvent`, `useTransform`.
2.  **Define Animation Sequence:** Use a `useEffect` or Framer Motion's `useAnimation` to sequence the events.
    *   *Alternative:* Use simple state variables to trigger different parts of the animation based on timeouts, or rely on Framer Motion's `delay` properties within initial/animate props. Using `delay` is often cleaner for linear sequences.
3.  **Rewrite Components:**
    *   **Logo & Shutters:** Initial state: visible, centered. Animate: fade out logo, split shutters (translate X).
    *   **COMPETITION Mask:** Initial state: scaled down, visible. Animate: scale up massively, fade out.
    *   **UI Reveal:** Initial state: hidden, shifted down. Animate: fade in, shift up (delayed to happen after the mask clears).
4.  **Update Container:** Change `h-[800vh]` to `h-screen` or `min-h-screen`. Ensure the video plays in the background continuously.
5.  **Trigger `onComplete`:** Call the `onComplete` callback when the sequence finishes so the rest of the page (like Navbar) can react if needed.

## Navbar Reversion
- If the Navbar was also tied to global scroll, we might need to revert it to appear based on the Hero completion state or a standard scroll offset (e.g., appear after scrolling past `100vh`). Given the user's prompt, we should make sure the Navbar appears gracefully after the Hero sequence.

## Plan Verification
1.  Review `Hero.tsx` for any remaining scroll dependencies.
2.  Ensure animations look premium (using cubic-bezier easings).
3.  Confirm the page flows logically into `StorySection` after the hero.